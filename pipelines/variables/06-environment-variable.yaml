trigger:
- main

# Define stages
stages:
- stage: Build
  jobs:
  - job: BuildJob
    displayName: "Build Job"
    steps:
    - script: echo "The current working directory is ${{ env.BUILD_SOURCESDIRECTORY }}"
